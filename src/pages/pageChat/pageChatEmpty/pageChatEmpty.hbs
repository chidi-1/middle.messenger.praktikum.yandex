<div class="aside">
    <div class="aside__header">
        <div class="text-right">
            <button class="js--toggle-data no-style">
                Профиль
                <span class="icon-angle-right"></span>
            </button>
        </div>
        {{{buttonAddChat}}}
        {{{formSearchChat}}}
        {{{chatList}}}
    </div>
</div>

<div id="chat" class="chat {{#unless isChatSelected}}empty{{/unless}}">
    <span class="fs-12 empty_text">Выберите чат чтобы отправить сообщение</span>

    {{{chatHeader}}}
    {{{chatContent}}}
    {{{chatFooter}}}
</div>

{{{modalAddChat}}}
